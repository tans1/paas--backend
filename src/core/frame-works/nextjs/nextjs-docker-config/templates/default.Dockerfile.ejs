# templates/nextjs/default.Dockerfile.ejs
FROM node:<%= nodeVersion %>-alpine
WORKDIR /app

ENV PORT <%= port %>

COPY package*.json ./
RUN <%= installCommand %>

COPY . .
RUN <%= buildCommand %>

EXPOSE <%= port %>

CMD <%= runCommand  %> -p <%= port %>